To run the experiment manually:

bash /path/to/Runner.sh <numDoors> <numExperiments>

If you already have data and you only want the statistics:

bash /path/to/Analyzer.sh 

There are a couple of absolute paths hard-coded:
Look at Runner.sh and Analyzer.sh